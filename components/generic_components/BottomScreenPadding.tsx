import { View, Text } from "react-native";
import React from "react";
import tw from "../../lib/tailwind";

type Props = {};

const BottomScreenPadding = (props: Props) => {
  return (
    <View style={tw`h-footer`}>

    </View>
  );
};

export default BottomScreenPadding;
