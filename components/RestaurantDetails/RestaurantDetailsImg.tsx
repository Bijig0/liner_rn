import { View, Text } from "react-native";
import React from "react";
import DetailsImg from "../generic_components/DetailsImg";

type Props = {};

const RestaurantDetailsImg = (props: Props) => {
  return (
    <DetailsImg type="viewDetails"/>
  );
};

export default RestaurantDetailsImg;
