const path = require("path");
const fs = require("fs");

console.log(path.join(__dirname, "Documents"))

// For every file in screens
// Get the default export for all of them
// The name of the file is the name of the screen
